# CodePath 后续规划

## 核心设想

CodePath 不只做 GitHub 源码解释器，还可以进一步成为“项目经验提炼器”。它可以把复杂 GitHub 项目中的某个功能，转化为适合人类开发者、OpenClaw、Hermes 等 agent 学习和复用的结构化知识。

目标不是简单总结源码，而是从源码中提炼：

- 功能使用的技术栈
- 关键实现路径
- 文件职责和调用关系
- 可迁移的工程模式
- 不适合照搬的项目特定细节
- 可用于新项目搭建的实现蓝图
- 可被 agent 消化的 `SKILL.md` 或任务说明

## 使用场景

### 人类开发者学习复杂项目

用户在 GitHub 页面上输入一个功能，例如“登录”“搜索”“插件系统”“缓存系统”“训练流程”或“多 agent 调度”，CodePath 输出该功能的技术栈、实现思路、关键文件和阅读路径。

### OpenClaw / Hermes 学习项目经验

CodePath 将源码分析结果压缩成结构化文档，供 OpenClaw 或 Hermes 读取，用于学习优秀项目的实现方式，优化自身能力，或者作为后续任务的长期上下文。

### 借鉴 GitHub 项目搭建新项目

用户可以选择“生成新项目蓝图”，让 CodePath 把某个项目中的功能抽象成可迁移方案，例如模块拆分、数据流、状态管理、接口设计、错误处理和测试建议。

## 规划能力

### 0. 基础体验和开发效率

在扩展能力继续变复杂之前，先降低日常开发和验证成本：

- 新增 `deploy:edge` 脚本，自动完成构建和复制产物。
- 固定真实浏览器加载目录，减少源码目录、构建目录和扩展目录不一致带来的误判。
- 每次修改侧边栏行为时更新可见构建版本，方便确认 Edge 加载的是新代码。
- 将“构建、复制、重新加载、刷新 GitHub 页面”的流程写入文档和脚本提示。
- 保持 `.output`、浏览器 profile、本地加载目录和交接文件不进入 Git。

### 1. 功能技术栈分析

输入目标功能后，CodePath 分析该功能相关源码，输出：

- 功能目标和用户价值
- 参与的前端、后端、状态管理、数据层、模型或工具库
- 关键入口文件
- 核心模块和辅助模块
- 数据流、调用链或执行流程
- 已确认事实与谨慎推断

### 2. 实现路径提炼

在现有“功能路径分析”的基础上，进一步抽象实现思路：

- 这个功能解决了什么问题
- 为什么项目采用这种模块拆分
- 关键设计取舍是什么
- 哪些实现可以迁移到新项目
- 哪些实现依赖当前项目上下文，不能直接照搬
- 如果从零实现，推荐的开发顺序是什么

### 3. Skill 生成器

新增面向 agent 的结构化输出，生成类似下面的文档：

```md
# Skill: 某项目中的搜索功能实现模式

## 适用场景

## 技术栈

## 实现路径

## 关键文件

## 可迁移思路

## 新项目搭建建议

## 风险与注意事项
```

该文档可以用于 OpenClaw、Hermes 或其他 agent 的上下文学习。

### 4. 新项目蓝图生成

基于被分析项目的功能实现，生成一个面向新项目的实现方案：

- 推荐目录结构
- 模块职责划分
- 数据结构和接口草案
- 关键组件或服务设计
- 开发步骤
- 测试重点
- 可选技术栈替代方案

### 5. 输出用途选择

后续可以在侧边栏新增“借鉴 / Skill”视图，用户选择输出用途：

- 给人读的技术分析
- 给 Hermes 学习的摘要
- 给 OpenClaw 执行的任务计划
- 新项目实现蓝图
- 可复制的 `SKILL.md`

### 6. 性能观测和提速

当前已经有总耗时显示，后续需要进一步拆分请求阶段，明确慢在哪里：

- GitHub 仓库信息加载耗时
- tree 加载耗时
- 文件内容读取耗时
- 上下文构造耗时
- 模型请求耗时
- 总耗时

这些数据可以用于判断瓶颈来自 GitHub、模型接口、上下文过大，还是扩展消息链路。

### 7. 缓存策略升级

当前缓存是内存级 `Map`，刷新页面或扩展重载后会丢。后续可以升级为可控持久化缓存：

- 按 `owner/repo/branch` 缓存项目树。
- 按 `owner/repo/branch/path` 缓存文件内容。
- 按问题、上下文 hash 和模型配置缓存追问结果。
- 显示回答来源：来自缓存、来自已有上下文、来自源码重新分析。
- 增加“清空当前仓库缓存”和“清空全部缓存”按钮。

### 8. 项目理解质量提升

现阶段主要依赖路径评分和关键文件抽样，后续可以加入更强的项目理解能力：

- README、package/config/entry 文件优先解析。
- 针对前端、后端、Python 应用、Python ML、库项目使用不同分析模板。
- 构建简化 import graph，辅助判断文件关系。
- 更细致地识别路由、状态管理、API 层、模型层、训练入口和插件入口。
- 在回答中明确“源码确认”“基于路径和命名的推断”“需要进一步读取源码确认”。

### 9. 问答体验升级

后续追问可以从“单次回答”升级为更接近研究工作台：

- 支持“只基于已有分析回答”。
- 支持“强制重新查源码回答”。
- 基于当前分析摘要和源码引用生成上下文相关的推荐追问。
- 问答历史可折叠、复制、清空。
- 回答中显示参考来源和来源类型。
- 对长回答提供摘要、详细版、可执行计划三种视图。

### 10. 错误处理和配置诊断

扩展需要更清晰地区分不同失败原因：

- GitHub rate limit 独立提示，并引导填写 GitHub Token。
- 模型 API Key 错误独立提示。
- Base URL、模型名称、网络失败、后台不可用分别给出解决动作。
- 设置页增加“测试模型连接”和“测试 GitHub 连接”的状态说明。
- 对超时请求显示当前阶段，减少“只是卡住了”的不确定感。

### 11. 本地项目分析模式

长期可以支持不依赖 GitHub 页面的本地项目分析：

- 用户选择本地目录后读取源码。
- 复用项目概览、功能路径、文件解释和 Skill 生成能力。
- 支持本地开发中的二次分析和重构规划。
- 对本地文件路径做隐私保护，避免输出或提交真实个人路径。

### 12. 多模型和多 agent 适配

在保持 Qwen 默认体验的基础上，增强 OpenAI-compatible 生态适配：

- 支持 OpenAI、Qwen、DeepSeek、智谱等兼容接口配置。
- 为不同模型提供推荐上下文长度和 prompt 策略。
- 增加面向 OpenClaw / Hermes 的输出模板。
- 后续可支持“复制为 agent 任务”“复制为 Skill”“复制为新项目计划”。

### 13. 结构化知识图谱

当源码理解能力稳定后，可以沉淀结构化视图：

- 文件职责图
- 功能调用链
- 入口文件地图
- 技术栈雷达
- 二次开发风险清单
- 新项目搭建蓝图

## 阶段路线

## 当前进展

- 已实现 CodePath MCP Server，可通过 `npm.cmd run mcp` 启动。
- OpenClaw 可通过 MCP 发现并调用 `analyze_github_project`、`analyze_github_feature`、`generate_openclaw_skill`、`generate_project_blueprint`。
- 已完成基础 smoke test：MCP 客户端可以列出全部工具。
- 已验证浏览器扩展仍可正常构建，MCP 能力与侧边栏 UI 解耦。
- 当前 MCP 调用仍依赖环境变量提供模型 API Key、Base URL、模型名称和可选 GitHub Token。

### 第一阶段：把规划文档化

- 新增本规划文档
- 明确 CodePath 从“源码解释器”升级为“项目经验提炼器”的方向
- 保持现有分析、追问、缓存和耗时显示能力稳定
- 新增或规划 `deploy:edge`，固定开发验证流程
- 状态：已完成。

### 第二阶段：稳定开发流程和性能观测

- 增加 `deploy:edge` 脚本
- 拆分耗时指标，显示 GitHub、文件读取、模型请求等阶段耗时
- 保留总耗时显示
- 将真实加载目录和重载流程写入文档
- 状态：已推进。已有 `deploy:edge` 脚本、总耗时显示、阶段耗时字段、Edge 真实加载目录文档；后续继续细化 UI 展示和真实项目耗时样本。

### 第三阶段：补强功能分析输出

- 在功能分析结果中加入“技术栈”和“可迁移思路”
- 区分“源码确认”和“工程推断”
- 输出关键文件的职责说明
- 支持复制 Markdown
- 状态：已推进。MCP 侧已新增 OpenClaw Skill / 新项目蓝图 prompt；浏览器侧已新增“借鉴 / Skill”入口和复制 Markdown 第一版。

### 第四阶段：缓存和问答体验升级

- 增加持久化缓存
- 显示回答来源类型
- 增加缓存清理入口
- 支持“仅基于已有分析回答”和“强制重新查源码”
- 状态：已推进。已有内存缓存，tree、文件内容、项目概览增加浏览器持久化缓存，设置页增加缓存清理入口；推荐追问已开始基于当前分析上下文生成；缓存策略和命中提示仍需继续打磨。

### 第五阶段：新增 Skill 输出

- 新增“生成 Skill”按钮
- 输出标准化 `SKILL.md` 结构
- 支持面向 OpenClaw / Hermes 的不同语气和粒度
- 在问答中允许用户继续追问“把这个 Skill 改成更适合新项目搭建”
- 状态：已推进。MCP 工具 `generate_openclaw_skill` 已实现；浏览器侧已新增生成入口和复制 Markdown 能力，下载 `SKILL.md` 待开发。

### 第六阶段：新增蓝图输出

- 支持“根据这个项目的某功能，生成新项目实现方案”
- 输出目录结构、模块边界、接口草案和开发步骤
- 标注可以复用的模式和不能照搬的细节
- 状态：MCP 工具 `generate_project_blueprint` 已实现；后续需要在 OpenClaw 实战中继续调优输出结构。

### 第七阶段：本地项目、多模型和知识图谱

- 支持本地项目分析
- 增强多模型配置和诊断
- 生成文件职责图、调用链和技术栈图
- 支持为 OpenClaw / Hermes 批量导出学习材料
- 状态：待开发。

### 第八阶段：沉淀可复用知识库

- 支持保存多个功能 Skill
- 支持导出 Markdown
- 后续可考虑本地知识库或长期缓存
- 为 OpenClaw / Hermes 提供批量学习材料
- 状态：待开发。

## 近期优先级

1. 增加 `deploy:edge` 脚本，自动构建并复制到浏览器加载目录。
2. 给 MCP 工具增加更清晰的结构化返回，减少 OpenClaw 解析 Markdown 的成本。
3. 拆分耗时指标，定位慢在 GitHub、源码读取、上下文构造还是模型请求。
4. 在浏览器侧新增“借鉴 / Skill”视图，复用 `generateSkillBlueprint`。
5. 增加“复制 OpenClaw 任务交接 Markdown”和“下载 Skill.md”能力。
6. 设计持久化缓存和“清空缓存”入口。
7. 优化错误提示和设置页连接诊断。
8. 用真实 OpenClaw 项目开发任务验证 `generate_openclaw_skill` 和 `generate_project_blueprint` 的可执行性。

## 设计原则

- 不直接复制项目代码，而是提炼工程经验。
- 不把推断伪装成事实，必须区分源码确认和谨慎推断。
- 生成内容要能被人读，也要能被 agent 消化。
- 输出要服务于二次开发、新项目搭建和 agent 学习。
- 始终避免提交 API Key、Token、本机路径或隐私信息。
